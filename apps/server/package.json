{
  "name": "server",
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "build": "tsdown",
    "check-types": "tsc -b",
    "compile": "bun build --compile --minify --sourcemap --bytecode ./src/index.ts --outfile server"
  },
  "dependencies": {
    "@ai-sdk/devtools": "^1.0.8",
    "@ai-sdk/google": "^4.0.27",
    "@notetaker-app/db": "workspace:*",
    "@notetaker-app/env": "workspace:*",
    "ai": "catalog:",
    "dotenv": "catalog:",
    "hono": "^4.12.32",
    "zod": "catalog:"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "catalog:",
    "@notetaker-app/config": "workspace:*",
    "@types/node": "catalog:",
    "tsdown": "^0.22.14",
    "typescript": "catalog:"
  }
}
